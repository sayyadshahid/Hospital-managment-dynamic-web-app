import { Box, Typography } from "@mui/material";

export default function SideBar () {
    return(
        <Box sx={{width: '30%', bgcolor: '#f38888', height: '100vh'}}>
            <Typography>
                USERS
            </Typography>
        </Box>
    )
}